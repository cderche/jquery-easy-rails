require "spec_helper"

describe Jquery::Easy::Rails do
  it "has a version number" do
    expect(Jquery::Easy::Rails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
